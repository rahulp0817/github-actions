name: Deploy NodeJS Application to AWS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Set up done
        run: |
          # sudo apt-get update
          # sudo apt-get install -y docker.io
          echo "Done GitHub Actions"